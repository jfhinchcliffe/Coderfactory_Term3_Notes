<h2><%= artist.name %></h2>
<% if (artist.images && artist.images.length > 0) { %>
<img src="<%= artist.images[0].url %>" width="300" > 
<% } %>   
<h3>Genres: <%= artist.genres.join(', ') %></h3>
<h4>Followers: <%= artist.followers.total %></h4>